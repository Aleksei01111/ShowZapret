<Page x:Class="WpfApp.Pages.CreateDonos.ConfirmLoginStepPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfApp.Pages.CreateDonos"
      mc:Ignorable="d"
      xmlns:uControls="clr-namespace:GeneralElementsUI.UserControls;assembly=GeneralElementsUI"
      Title="ConfirmLoginStepPage" d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=local:ConfirmLoginStepPage, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition/>
            <RowDefinition Height="0.3*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" Style="{StaticResource Head2}" Text="Подтвердите данные учетной записи"/>
    
        <StackPanel Grid.Row="1">
            <uControls:TextBoxWithPlaceholder Placeholder="Логин" Text="{Binding UserName}"/>
            <uControls:PasswordBoxWithPlaceholder 
                Placeholder="Пароль"
                Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        
        <Button Grid.Row="2" Style="{StaticResource Button1}" Click="Done_OnClick" Content="Дальше"/>
    </Grid>
</Page>
